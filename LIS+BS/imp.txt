Its the pattern of doing bs when the temp.back()>nums[i] so we put nums i on its place

if strictly greater use lower lower_bound else use uppr bound
use it as mountain array efficiently
also we can use pair dp


questions are
300
354
1964
1671
1713
1211

======
673






















































/*

Russian Doll


class Solution {
public:
    int maxEnvelopes(vector<vector<int>>& envelopes) {
        int n=envelopes.size();
        sort(envelopes.begin(),envelopes.end(),[&](vector<int>&a,vector<int>&b){
              if(a[0]<b[0]) return true;
              if(a[0]==b[0]) return  b[1]<a[1];
              return false;
        });
        vector<int>nums,temp;
        for(auto &v:envelopes){
          nums.push_back(v[1]);
        }
           int ans=0;
           for(int i=0;i<n;i++ ){
               int m=temp.size();
    
         if(temp.size()==0){
            temp.push_back(nums[i]);
            ans=1;
         }
         else if(temp[m-1]<nums[i]){
            temp.push_back(nums[i]);
            ans=max(ans,m+1);
         }
                else {
                    auto it=lower_bound(
                        temp.begin(),
                        temp.end(),
                        nums[i]
                    );
                    int idx=it-temp.begin();
                    temp[idx]=nums[i];
                ans=max(ans,idx+1);
                }

           }

           return ans;
    }
};
*/
